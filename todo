Task: improve make target
- i want the make dev command to be called make devstack (and related commands be the same)
- i want there to be a make devstack that also deletes the old db and recreates a new one populated with data

Task: Make Tags Clearer
- adding of tags to be more clear
- we should be able to see other tags when adding a new tag, so that we can know suitable things to include this as
- maybe a similar kind of design as how labels work in github? and when adding new tags there is a search bar that would
  reflect how much the tag is used
- add a make command to download a fresh copy of the db to develop against

